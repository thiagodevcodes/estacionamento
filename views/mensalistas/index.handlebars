<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mensalistas</title>
</head>
<body class="layout">
    <header class="header-main">

      <nav>
        <div class="logo">
          <img src="/img/logo.png" alt="">
          <h1>Bem vindo(a), {{user.login}}</h1>
        </div>

        <div class="mobile-menu">
          <div class="line1"></div>
          <div class="line2"></div>
          <div class="line3"></div>
        </div>
        <ul class="nav-list">
            <li class="button-sair"><img src="/img/user-logo.png" alt=""><a class="button-nav" href="/login/logout">Sair</a></li>
        </ul>
      </nav>

    </header>

    <aside class="aside-main">
        <nav>
            <ul>
                <li><img src="/img/mensalista.png" alt=""><a href="/mensalistas">Mensalistas</a></li>
                <li><img src="/img/rotativos.png" alt=""><a href="/rotativos">Rotativos</a></li>
                <li><img src="/img/vaga.png" alt=""><a href="/vagas">Vagas</a></li>
                {{#if user.admin}}
                    <li><img src="/img/users.png" alt=""><a href="/users">Usuários</a></li>
                {{/if}}
            </ul>
        </nav>
    </aside>

    <div class="modal" id="modalAluno">
        <div class="modal-content">
            <header class="modal-header">
                <img src="/img/mensal.png" class="modal-img" alt="">
                <h2 style="font-weight: bold;">Novo Mensalista</h2>
            </header>
            <form action="/mensalistas" method="POST" class="modal-form">
                <input class="modal-field" placeholder="Nome" type="text" name="nome" required/> 
                <input class="modal-field" placeholder="CPF" type="text" name="cpf" required/> 
                <input class="modal-field" placeholder="Telefone" type="text" name="telefone"required/>
                <input class="modal-field" placeholder="Email" type="text" name="email" required/>
                <input class="modal-field" placeholder="Dia de Vencimento" type="text" name="diavencimento"required/>
                <select name="vagas" id="select">
                    {{#each vagas}}
                        <option value="{{id}}">{{id}}</option>
                    {{/each}}
                </select>
                <div class="modal-buttons">
                    <button class="button green" type="submit">Enviar</button>
                    <button class="button green" id="cadClose" onclick="ModalAluno()" type="button">Fechar</button>
                </div>
            </form>
        </div>
    </div>
  
    <main class="main">

    <div class="main-group">
        <button onclick="ModalAluno()"><img src="/img/add.png" alt=""> Adicionar Novo Mensalista</button>
        <input id="searchbar" placeholder="Buscar..."/>
    </div>

    <div class="tables-container">
        <table id="tabela" class="tables-table">
            <thead >
                <tr class="table-head">
                    <th>Nome</th>
                    <th>CPF</th>
                    <th>Telefone</th>
                    <th>Email</th>
                    <th>Vencimento</th>
                    <th>Admissão</th>
                    <th colspan="2">Opções</th>
                </tr>
            </thead>
        
            <tbody id="body-table"> 
                {{#each posts}}  
                    {{#if dataRecisao}}
                    {{else}} 
                        <tr>
                            <td>{{nome}}</td>
                            <td>{{cpf}}</td>
                            <td>{{telefone}}</td>
                            <td>{{email}}</td>
                            <td>{{diaVencimento}}</td>
                            <td>{{dataAdmissao}}</td>
                            <td class="td-button">
                                <a href="/mensalistas/{{id}}"><button><img src="/img/pencil.png" alt=""></button></a> 
                            </td>

                            <td class="td-button">
                                <a href="/mensalistas/finalizar/{{id}}"><button><img src="/img/checkb.png" alt=""></button></a>
                            </td>
                        </tr>
                    {{/if}}			
                {{/each}}
            </tbody>
        </table>            
    </div>
    </main>
    <script type="text/javascript" src="http://code.jquery.com/jquery-1.7.2.min.js"></script>
    <script src="/js/main.js"></script>    
</body>
</html>



