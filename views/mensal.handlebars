<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <header>
      <nav>
        <div class="logo">
          <img src="/img/carro.png" alt="">
          <h1>Bem vindo(a), {{user.login}}</h1>
        </div>

        <div class="mobile-menu">
          <div class="line1"></div>
          <div class="line2"></div>
          <div class="line3"></div>
        </div>
        <ul class="nav-list">
          <li><button class="button-nav" onclick="ModalAluno()">Cadastrar Mensalista</button></li>
          <li><a class="button-nav" href="/">Rotativos</a></li>
          <li><a class="button-nav" href="/mensalista">Mensalistas</a></li>
          {{#if user.admin}}
            <li><a class="button-nav" href="/users">Usuários</a></li>
            
          {{/if}}
          <li><a class="button-nav" href="/login/logout">Sair</a></li>
        </ul>
      </nav>
    </header>

    <div class="modal" id="modalAluno">
        <div class="modal-content">
            <header class="modal-header">
                <img src="/img/servico-de-gestao.png" class="modal-img" alt="">
                <h2 style="font-weight: bold;">Novo Mensalista</h2>
            </header>
            <form action="/mensalista" method="POST" class="modal-form">
                <input class="modal-field" placeholder="Nome" type="text" name="nome" required/> 
                <input class="modal-field" placeholder="CPF" type="text" name="cpf" required/> 
                <input class="modal-field" placeholder="Telefone" type="text" name="telefone"required/>
                <input class="modal-field" placeholder="Email" type="text" name="email" required/>
                <input class="modal-field" placeholder="Dia de Vencimento" type="text" name="diavencimento"required/>
                <div class="modal-buttons">
                    <button class="button green" type="submit">Enviar</button>
                    <button class="button green" id="cadClose" onclick="ModalAluno()" type="button">Fechar</button>
                </div>
            </form>
        </div>
    </div>
    
    
    <div class="registros-group">
        <input class="registros-search" id="searchbar" placeholder="Buscar..."/>
    </div>

    <div class="registros-mat">
        <table id="tabela" class="table">
            <thead >
                <tr class="table-head">
                    <th>Nome</th>
                    <th>CPF</th>
                    <th>Telefone</th>
                    <th>Email</th>
                    <th>Dia de Vencimento</th>
                    <th>Data de Admissão</th>
                    <th colspan="2">Opções</th>
                </tr>
            </thead>
        
            <tbody id="body-table"> 
                {{#each posts}}  
                    {{#if dataRecisao}}
                    {{else}} 
                        <tr>
                            <td>{{nome}}</td>
                            <td>{{cpf}}</td>
                            <td>{{telefone}}</td>
                            <td>{{email}}</td>
                            <td>{{diaVencimento}}</td>
                            <td>{{dataAdmissao}}</td>
                            <td class="td-button">
                                <a href="/mensalista/{{id}}"><button class="registros-button"><img class="img-button" width="20px" src="/img/lapis.png" alt=""></button></a> 
                            </td>

                            <td class="td-button">
                                <a href="/mensalista/finalizar/{{id}}"><button class="registros-button"><img class="img-button" style="filter: invert(1);" width="20px" src="/img/lixeira.png" alt=""></button></a>
                            </td>
                        </tr>
                    {{/if}}			
                {{/each}}
            </tbody>
        </table>            
    </div>

    <script type="text/javascript" src="http://code.jquery.com/jquery-1.7.2.min.js"></script>
    <script src="/js/main.js"></script>    
</body>
</html>



